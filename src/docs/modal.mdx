---
title: Modal
description: A reusable modal component for creating accessible and animated dialog boxes.
---

import { motionSnippets, hooksSnippets, variantsCodeSnippet } from "@/components/showcase/modal/snippets"
import { useIsMountedHook } from "@/components/showcase/shared-snippets/snippets"

<section>

  <Tabs mountOnChange>
    <TabPanel label="Preview">
        <ComponentCard>
            <ModalDemo />
        </ComponentCard>
    </TabPanel>
    <TabPanel label="Code">
        <CodeSnippet code={motionSnippets.code} language="tsx"/>
    </TabPanel>
    <TabPanel label="Usage">
        <CodeSnippet code={motionSnippets.usage} language="tsx"/>
    </TabPanel>
  </Tabs>

</section>

<section>
  <header>
    ## Custom hooks
  </header>

  <Tabs mountOnChange>
    {hooksSnippets.map((hook) => {
      return <TabPanel key={hook.id} label={hook.name}><CodeSnippet code={hook.code} language="typescript"/></TabPanel>
    })}
  </Tabs>

  Required if you're using server components:

  <Tabs mountOnChange>
      <TabPanel label={useIsMountedHook.name}><CodeSnippet code={useIsMountedHook.code} language="typescript"/></TabPanel>
  </Tabs>

</section>


<section>
  <header>
    ## More examples
    Keep in mind that you can make more combinations and even add your own variants! 
  </header>

  <Tabs>
    <TabPanel label="Variants">
        <ComponentCard>
            <ModalVariantsDemo />
        </ComponentCard>
    </TabPanel>
    <TabPanel label="Code">
        <CodeSnippet code={variantsCodeSnippet} language="tsx" disableCopy/>
    </TabPanel>
  </Tabs>

</section>

<section>
  <header>
    ## Dependencies
    This component relies on the following libraries:
  </header>

  - **<a href="https://tailwindcss.com/" target="_blank">Tailwind CSS</a>**
  - **<a href="https://github.com/dcastil/tailwind-merge" target="_blank">clsx</a>**
  - **<a href="https://github.com/lukeed/clsx" target="_blank">tailwind-merge</a>**
  - **<a href="https://cva.style/" target="_blank">Class Variance Authority</a>**
  - **<a href="https://motion.dev/" target="_blank">Motion</a>**

</section>
